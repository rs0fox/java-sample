version: 0.2

phases:
  install:
    
    commands:
      - mvn clean install -f pom.xml
      - ls -al  # List all files and directories to verify structure

  pre_build:
    commands:
      - pwd
      - ls -al  # List all files and directories to verify structure
      - if [ -d "scripts" ]; then cd scripts; fi  # Change directory to 'scripts' if it exists
      - ls -al  # List files and directories inside 'scripts' directory if changed

  build:
    commands:
      - ls -al  # List all files and directories to verify structure
      - # Additional build commands as needed

  post_build:
    commands:
      - pwd
      - ls -al  # List all files and directories to verify structure
      - if [ -d "scripts" ]; then cd scripts; fi  # Change directory to 'scripts' if it exists
      - ls -al  # List files and directories inside 'scripts' directory if changed
      - chmod +x *.sh  # Ensure scripts are executable
      - ./start_server.sh  # Execute start_server.sh script
      - ./stop_server.sh   # Execute stop_server.sh script after deployment

artifacts:
  files:
    - '**/*'
